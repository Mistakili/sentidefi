{
  "version": 1,
  "sdk_version": "0.20.1",
  "path": "/mcp",
  "auth": {
    "type": "none"
  },
  "mcp": {
    "server": {
      "name": "sentinelfi-mcp",
      "version": "0.1.0",
      "title": "SentinelFi"
    },
    "tools": [
      {
        "name": "check_trust",
        "title": "Check trust (SentinelFi Trust Protocol)",
        "description": "The trust checkpoint every AI agent, wallet, or protocol should call before executing an on-chain action. Returns a clear recommendation (Proceed / Proceed with Caution / Manual Review Required / Block), a Trust Grade (A–F), a risk score, plain-English reasoning, and a cryptographically signed Safety Attestation any third party can verify off-chain with only the attestor's public address. HSK Chain live today; BotChain and additional EVMs plug into the same interface.",
        "annotations": {
          "readOnlyHint": true,
          "idempotentHint": false,
          "openWorldHint": true
        },
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "chainId": {
              "type": "integer",
              "minimum": -9007199254740991,
              "maximum": 9007199254740991,
              "description": "EVM chain id (177 = HSK, 677 = BotChain Mainnet, 968 = BotChain Testnet)."
            },
            "action": {
              "type": "string",
              "enum": [
                "swap",
                "approve",
                "transfer",
                "contract_call"
              ],
              "description": "The kind of on-chain action being evaluated."
            },
            "contract": {
              "description": "Target contract address (token, protocol, or destination).",
              "type": "string",
              "pattern": "^0x[0-9a-fA-F]{40}$"
            },
            "wallet": {
              "description": "Wallet initiating the action (optional, used for context).",
              "type": "string",
              "pattern": "^0x[0-9a-fA-F]{40}$"
            },
            "token": {
              "description": "Token address if different from `contract`.",
              "type": "string",
              "pattern": "^0x[0-9a-fA-F]{40}$"
            },
            "agentId": {
              "description": "Stable identifier of the calling agent (for future reputation).",
              "type": "string",
              "maxLength": 128
            },
            "anchor": {
              "description": "If true, also anchor the Safety Attestation on-chain (BotChain RiskRegistry). Requires BotChain configuration.",
              "type": "boolean"
            }
          },
          "required": [
            "chainId",
            "action"
          ]
        },
        "outputSchema": null
      },
      {
        "name": "scan_token",
        "title": "Scan HSK token",
        "description": "Fetch live on-chain data for a token contract on HSK Chain (chainId 177): whether the address is a contract or EOA, ERC20 metadata (name, symbol, decimals, totalSupply), and bytecode size. Use as the first step for any risk analysis of a token address.",
        "annotations": {
          "readOnlyHint": true,
          "idempotentHint": true,
          "openWorldHint": true
        },
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "address": {
              "type": "string",
              "pattern": "^0x[0-9a-fA-F]{40}$",
              "description": "0x-prefixed 40-char hex token contract address on HSK Chain"
            }
          },
          "required": [
            "address"
          ]
        },
        "outputSchema": null
      },
      {
        "name": "get_wallet_portfolio",
        "title": "Get wallet portfolio",
        "description": "Read a wallet's live on-chain portfolio on HSK Chain (or another supported EVM chain by chainId): native balance, tracked ERC20 balances, per-token USD prices, and total USD value.",
        "annotations": {
          "readOnlyHint": true,
          "idempotentHint": true,
          "openWorldHint": true
        },
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "address": {
              "type": "string",
              "pattern": "^0x[0-9a-fA-F]{40}$",
              "description": "0x-prefixed 40-char hex wallet address"
            },
            "chainId": {
              "description": "EVM chain id (default 177 for HSK Chain).",
              "type": "integer",
              "minimum": -9007199254740991,
              "maximum": 9007199254740991
            }
          },
          "required": [
            "address"
          ]
        },
        "outputSchema": null
      },
      {
        "name": "list_recent_risk_scans",
        "title": "List recent risk scans",
        "description": "Read the SentinelFi public risk-scan feed: the most recent AI-generated risk verdicts for HSK Chain token contracts, including score (0-100), level (LOW/MEDIUM/HIGH/CRITICAL), a plain-English summary, and — when attested — the on-chain transaction hash.",
        "annotations": {
          "readOnlyHint": true,
          "idempotentHint": false,
          "openWorldHint": true
        },
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "limit": {
              "description": "How many recent scans to return (1-50, default 10).",
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            },
            "address": {
              "description": "Optional token address filter — return only scans for this contract.",
              "type": "string",
              "pattern": "^0x[0-9a-fA-F]{40}$"
            }
          }
        },
        "outputSchema": null
      }
    ]
  }
}
