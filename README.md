# SentinelFi

**MCP-native risk infrastructure for EVM DeFi.**
The agent-callable safety layer for HSK, BotChain, and every EVM chain.

> One public MCP endpoint. Three read-only tools. Any wallet, protocol, or AI
> assistant plugs in in minutes — no SDK, no keys, no scraping.

- 🌐 **Live app:** https://sentidefi.lovable.app
- 📖 **Whitepaper / docs:** https://sentidefi.lovable.app/docs
- 🤖 **MCP endpoint:** https://sentidefi.lovable.app/mcp
- 🏆 Built for the **HSK On-Chain Horizon Hackathon 2026**

---

## What is SentinelFi?

Every new chain hits the same cold-start problem: wallets won't integrate
without safety data, safety data doesn't exist without scanners, and scanners
don't build for small chains. Meanwhile the existing scanners (GoPlus, De.Fi,
Honeypot.is) are **websites, not protocols** — nothing else can plug in.

SentinelFi is built the opposite way: **infrastructure first, UI second**.
The same engine that powers our Copilot ships as a public MCP server, so any
wallet, launchpad, DEX, or AI assistant can call it directly. HSK is the
launch chain; the architecture is chain-agnostic from day one.

---

## Why it matters

| Audience | What SentinelFi gives them |
|---|---|
| **Wallets** (MetaMask, Rabby, HashKey Wallet…) | Pre-transaction risk warnings via one MCP call — no in-house scanner to maintain. |
| **Protocols** (DEXs, launchpads, lending) | A shared trust primitive — gate listings, flag pools, score collateral. |
| **Chains** (HSK, BotChain, and beyond) | Instant safety infra so ecosystems don't stay cold waiting for GoPlus to notice them. |
| **AI agents** (Claude, ChatGPT, Cursor, Codex) | Native "is this token safe?" answers grounded in live on-chain data. |

---

## Features

- **MCP-native.** Public `/mcp` endpoint, zero-auth read tools.
- **HSK-first, chain-agnostic core.** New EVMs drop in via `src/lib/chains.ts`.
- **Contract X-ray.** Live on-chain reads: metadata, supply, contract-vs-EOA, bytecode.
- **AI Copilot.** Streaming chat that routes intent — token scan vs wallet lookup vs history.
- **Public risk-scan dataset.** Every verdict is written to a public feed with a tx hash for provenance.
- **Explainable verdicts.** Every score cites the on-chain evidence it used — no black box.
- **Portfolio insight.** Wallet balances, USD values, concentration & stablecoin ratio.

---

## MCP tools

Point Claude, ChatGPT, Cursor, Codex, or any MCP-aware client at
`https://sentidefi.lovable.app/mcp` and these tools appear automatically:

| Tool | What it does |
|---|---|
| `scan_token` | Live on-chain read for any EVM contract: metadata, supply, contract-vs-EOA, bytecode size. Returns a structured verdict. |
| `get_wallet_portfolio` | Native + tracked ERC20 balances with USD values and total for any address. |
| `list_recent_risk_scans` | The public verdict feed. Optional address filter. Score, level, summary, tx hash. |

All tools are `readOnlyHint: true` and only expose already-public data.

**Example (in any MCP client):**

```
> scan token 0x7f3a...b91e on HSK
→ score: 82  HIGH RISK
  reason: unlocked LP, 64% top holder

> what's in wallet 0xb9C5...Ded66?
→ HSK: 12.4  USDT: 830  total $1,204
```

---

## Tech stack

- **Framework:** TanStack Start v1 (React 19, Vite 7, SSR on Cloudflare Workers)
- **UI:** Tailwind CSS v4, shadcn/ui, streaming AI SDK chat
- **AI:** Lovable AI Gateway (tool calling, streaming)
- **MCP:** `@lovable.dev/mcp-js` mounted at `/mcp`
- **Data:** Direct JSON-RPC to HSK for balances, metadata, bytecode — no third-party indexer
- **Backend:** Lovable Cloud (Postgres + RLS) for the public risk-scan feed
- **Wallet:** EIP-1193 (MetaMask & compatible), multi-chain aware

---

## Repository layout

```
src/
├── routes/           TanStack Start file-based routes
│   ├── index.tsx       Landing page
│   ├── copilot.tsx     AI Copilot chat UI
│   ├── portfolio.tsx   Wallet portfolio view
│   ├── docs.tsx        Whitepaper
│   ├── mcp.ts          MCP endpoint (auto-generated by mcp-js)
│   └── api/chat.ts     Streaming chat server route
├── lib/
│   ├── mcp/            MCP server definition + tools
│   │   ├── index.ts      defineMcp entry
│   │   └── tools/        scan-token, get-wallet-portfolio, list-recent-risk-scans
│   ├── chains.ts       Chain-agnostic registry (HSK, BotChain, …)
│   ├── hsk-rpc.server.ts     On-chain reads
│   └── portfolio.server.ts   Wallet portfolio logic
├── components/       Wallet button, chat UI, shadcn primitives
└── integrations/     Supabase clients (auto-generated)
contracts/            RiskRegistry Solidity (roadmap)
supabase/             Migrations for the public risk_scans table
```

---

## Local development

```bash
bun install
bun run dev
```

The dev server runs at `http://localhost:8080`. The MCP endpoint is available
at `http://localhost:8080/mcp` and the same three tools work locally.

---

## Supported chains

SentinelFi is chain-agnostic. Adding a new EVM chain is a single entry in
`src/lib/chains.ts` — chain id, RPC URL, explorer, tracked tokens.

- **HSK Chain** (chainId 177) — live
- **HSK Testnet** (chainId 133) — used for on-chain attestation
- **BotChain** — scaffolded, enable per launch
- **Any EVM** (Base, BSC, Arbitrum, …) — drop-in via `chains.ts`

---

## Roadmap

**Shipped**
- Public MCP server with 3 read-only tools
- HSK-native on-chain reads (metadata, supply, contract-vs-EOA, bytecode)
- Chain-agnostic registry (`chains.ts`)
- Copilot with intent routing (token vs wallet vs history)
- Public risk-scan dataset with tx-hash provenance

**Next**
- **Deeper scoring** — liquidity locks, ownership renouncement, mint/blacklist/pause detection, honeypot + tax simulation
- **Behavioral signals** — whale-movement patterns, deployer history, rug-pattern matching from the public dataset
- **Open methodology** — versioned, auditable, open-source scoring rulesets
- **Multi-chain rollout** — BotChain, Base, BSC, and beyond on the same MCP surface
- **One-click strategy execution** via HSK DEX router
- **Continuous monitoring** — alerts when a held token's verdict changes

---

## FAQ

**Does SentinelFi ever move funds?**
No. All tools are read-only. Any transaction still requires the user to sign in their wallet.

**Where do risk scores come from?**
From live on-chain data plus LLM reasoning against explicit heuristics (contract-vs-EOA, ERC20 conformance, bytecode size, supply anomalies). Scores are opinions, not guarantees — always DYOR.

**Is the MCP server really public?**
Yes. No auth, no keys, no rate-limit paperwork. All three tools only read already-public data (on-chain reads + the public `risk_scans` feed).

**How do I connect it to Claude / ChatGPT / Cursor?**
Add `https://sentidefi.lovable.app/mcp` as an MCP server in your client. The three tools appear automatically.

---

## License

To be announced after the hackathon. Scanning heuristics, the MCP server, and
the `RiskRegistry` contract will be published under a permissive license.

---

_Built with [Lovable](https://lovable.dev) for the HSK On-Chain Horizon Hackathon 2026._